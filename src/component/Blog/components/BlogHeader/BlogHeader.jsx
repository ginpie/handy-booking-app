import React from "react";
import styled from "styled-components";

const BlogHeader = () => {
  return (
    <div>
      <BlogHeader scrollAnime={true} />
    </div>
  );
};

export default BlogHeader;
