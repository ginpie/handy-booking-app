import React from "react";

import TableHeader from "../TableHeader";
import TableContent from "../TableContent";
const SignInTable = () => (
  <div>
    <TableHeader> Create A New Account</TableHeader>
    <TableContent />
  </div>
);
export default SignInTable;
